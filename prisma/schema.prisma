generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model Branch {
  id                 String               @id @default(cuid())
  name               String               @unique
  index              Int
  createdAt          DateTime             @default(now())
  updatedAt          DateTime             @updatedAt
  campaignBranches   CampaignBranch[]
  combinedBranches   CombinedBranch[]
  customerPurchases  CustomerPurchase[]
  customers          Customer[]
  dailyMetrics       DailyMetric[]
  dailyVisitors      DailyVisitor[]
  hourlyUsages       HourlyUsage[]
  strategyBranches   StrategyBranch[]
  ticketRevenues     TicketRevenue[]
  users              User[]
  customerClaims     CustomerClaim[]
  automationFlows    AutomationFlow[]

  @@map("branches")
}

model CampaignBranch {
  id         String   @id @default(cuid())
  campaignId String
  branchId   String
  createdAt  DateTime @default(now())
  branch     Branch   @relation(fields: [branchId], references: [id], onDelete: Cascade)
  campaign   Campaign @relation(fields: [campaignId], references: [id], onDelete: Cascade)

  @@unique([campaignId, branchId])
  @@index([branchId])
  @@index([campaignId])
  @@map("campaign_branches")
}

model Campaign {
  id               String           @id @default(cuid())
  name             String
  startDate        DateTime         @db.Date
  endDate          DateTime         @db.Date
  impressions      Int?
  clicks           Int?
  cost             Decimal?         @db.Decimal(10, 2)
  description      String?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
  status           String           @default("ONGOING")
  branches         CampaignBranch[]

  @@index([startDate, endDate])
  @@index([status])
  @@map("campaigns")
}

model CombinedAnalysis {
  id              String           @id @default(cuid())
  name            String
  startDate       DateTime         @db.Date
  endDate         DateTime         @db.Date
  cost            Decimal?         @db.Decimal(10, 2)
  impressions     Int?
  clicks          Int?
  strategyType    String?
  reason          String?
  description     String?
  status          String           @default("ONGOING")
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  branches        CombinedBranch[]

  @@index([startDate, endDate])
  @@index([status])
  @@map("combined_analyses")
}

model CombinedBranch {
  id         String           @id @default(cuid())
  combinedId String
  branchId   String
  createdAt  DateTime         @default(now())
  branch     Branch           @relation(fields: [branchId], references: [id], onDelete: Cascade)
  combined   CombinedAnalysis @relation(fields: [combinedId], references: [id], onDelete: Cascade)

  @@unique([combinedId, branchId])
  @@index([branchId])
  @@index([combinedId])
  @@map("combined_branches")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model CustomerPurchase {
  id           String   @id @default(cuid())
  customerId   String
  branchId     String
  purchaseDate DateTime @db.Date
  ticketName   String
  amount       Decimal  @db.Decimal(10, 2)
  paymentTime  String?
  pointUsed    Decimal? @db.Decimal(10, 2)
  branch       Branch   @relation(fields: [branchId], references: [id])
  customer     Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)

  @@index([branchId, purchaseDate])
  @@index([customerId])
  @@index([purchaseDate])
  @@index([ticketName])
  @@index([customerId], map: "idx_customer_purchases_customerid")
  @@index([purchaseDate], map: "idx_customer_purchases_purchasedate")
  @@map("customer_purchases")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model Customer {
  id                String             @id @default(cuid())
  phone             String             @unique
  gender            String?
  birthdate         String?
  age               Int?
  ageGroup          String?
  firstVisitDate    DateTime           @db.Date
  lastVisitDate     DateTime?          @db.Date
  lastPurchaseDate  DateTime?          @db.Date
  mainBranchId      String?
  totalVisits       Int                @default(0)
  totalSpent        Decimal            @default(0) @db.Decimal(10, 2)
  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  purchases         CustomerPurchase[]
  mainBranch        Branch?            @relation(fields: [mainBranchId], references: [id])
  dailyVisitors     DailyVisitor[]
  memos             CustomerMemo[]
  claims            CustomerClaim[]

  @@index([lastVisitDate])
  @@index([mainBranchId])
  @@index([phone])
  @@index([totalSpent])
  @@index([totalVisits])
  @@index([mainBranchId], map: "idx_customers_mainbranchid")
  @@index([phone], map: "idx_customers_phone")
  @@map("customers")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model DailyMetric {
  id                String   @id(map: "daily_metrics_new_pkey1") @default(cuid())
  branchId          String
  date              DateTime @db.Date
  newUsers          Int?
  seatUsage         Int?
  totalRevenue      Decimal? @db.Decimal(10, 2)
  dayTicketRevenue  Decimal? @db.Decimal(10, 2)
  timeTicketRevenue Decimal? @db.Decimal(10, 2)
  termTicketRevenue Decimal? @db.Decimal(10, 2)
  lockerRevenue     Decimal? @db.Decimal(10, 2)
  pointUsed         Decimal? @db.Decimal(10, 2)
  collectedAt       DateTime @default(now()) @db.Timestamp(6)
  newUsers10s       Int?
  newUsers20s       Int?
  newUsers30s       Int?
  newUsers40s       Int?
  newUsers50s       Int?
  newUsers60plus    Int?
  newUsersFemale    Int?
  newUsersMale      Int?
  branch            Branch   @relation(fields: [branchId], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "daily_metrics_branchid_fkey")

  @@unique([branchId, date], map: "daily_metrics_branchid_date_key")
  @@index([branchId, date], map: "daily_metrics_branchid_date_idx")
  @@index([date])
  @@map("daily_metrics")
}

model DailyVisitor {
  id                   String    @id @default(cuid())
  branchId             String
  visitDate            DateTime  @db.Date
  phone                String
  collectedAt          DateTime  @default(now())
  visitTime            String?
  age                  Int?
  ageGroup             String?
  gender               String?
  birthdate            String?
  duration             Int?
  endTime              DateTime?
  paymentMethod        String?
  remainingTermTicket  String?
  remainingTimePackage String?
  seat                 String?
  startTime            DateTime?
  customerId           String?
  remainingFixedSeat   String?
  branch               Branch    @relation(fields: [branchId], references: [id], onDelete: Cascade)
  customer             Customer? @relation(fields: [customerId], references: [id])

  @@index([branchId, visitDate])
  @@index([customerId])
  @@index([phone])
  @@index([visitDate])
  @@map("daily_visitors")
}

model HourlyUsage {
  id          String   @id @default(cuid())
  branchId    String
  date        DateTime @db.Date
  hour        Int
  usageCount  Int
  collectedAt DateTime @default(now())
  branch      Branch   @relation(fields: [branchId], references: [id], onDelete: Cascade)

  @@unique([branchId, date, hour])
  @@index([branchId, date])
  @@index([date])
  @@map("hourly_usage")
}

model Strategy {
  id               String           @id @default(cuid())
  name             String
  type             String
  startDate        DateTime         @db.Date
  endDate          DateTime         @db.Date
  description      String?
  createdAt        DateTime         @default(now())
  updatedAt        DateTime         @updatedAt
  reason           String?
  status           String           @default("ONGOING")
  branches         StrategyBranch[]

  @@index([startDate, endDate])
  @@index([status])
  @@index([type])
  @@map("strategies")
}

model StrategyBranch {
  id         String   @id @default(cuid())
  strategyId String
  branchId   String
  createdAt  DateTime @default(now())
  branch     Branch   @relation(fields: [branchId], references: [id], onDelete: Cascade)
  strategy   Strategy @relation(fields: [strategyId], references: [id], onDelete: Cascade)

  @@unique([strategyId, branchId])
  @@index([branchId])
  @@index([strategyId])
  @@map("strategy_branches")
}

model TicketBuyer {
  id              String        @id @default(cuid())
  ticketRevenueId String
  branchId        String
  date            DateTime      @db.Date
  ticketName      String
  collectedAt     DateTime      @default(now())
  customerHash    String?
  paymentTime     String?
  phone           String?
  amount          Decimal?      @db.Decimal(10, 2)
  pointUsed       Decimal?      @db.Decimal(10, 2)
  ticketRevenue   TicketRevenue @relation(fields: [ticketRevenueId], references: [id], onDelete: Cascade)

  @@index([branchId, date])
  @@index([customerHash])
  @@index([phone])
  @@index([ticketName])
  @@map("ticket_buyers")
}

model TicketRevenue {
  id            String        @id @default(cuid())
  branchId      String
  date          DateTime      @db.Date
  ticketName    String
  revenue       Decimal       @db.Decimal(10, 2)
  collectedAt   DateTime      @default(now())
  ticketBuyers  TicketBuyer[]
  branch        Branch        @relation(fields: [branchId], references: [id], onDelete: Cascade)

  @@unique([branchId, date, ticketName])
  @@index([branchId, date])
  @@index([date])
  @@index([ticketName])
  @@map("ticket_revenue")
}

model User {
  id        String    @id @default(cuid())
  username  String    @unique
  password  String
  name      String
  role      UserRole  @default(BRANCH)
  branchId  String?
  isActive  Boolean   @default(true)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  branch            Branch?            @relation(fields: [branchId], references: [id])
  memos             CustomerMemo[]
  claims            CustomerClaim[]
  savedFilters      SavedFilter[]
  automationFlows   AutomationFlow[]

  @@map("users")
}

model CustomerMemo {
  id         String   @id @default(cuid())
  customerId String
  content    String
  createdBy  String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  customer   Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
  author     User     @relation(fields: [createdBy], references: [id])

  @@index([customerId])
  @@index([createdBy])
  @@map("customer_memos")
}

model CustomerClaim {
  id          String    @id @default(cuid())
  customerId  String
  title       String
  description String?
  status      ClaimStatus @default(OPEN)
  branchId    String
  createdBy   String
  resolvedAt  DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  customer    Customer  @relation(fields: [customerId], references: [id], onDelete: Cascade)
  branch      Branch    @relation(fields: [branchId], references: [id])
  author      User      @relation(fields: [createdBy], references: [id])

  @@index([customerId])
  @@index([branchId])
  @@index([createdBy])
  @@index([status])
  @@map("customer_claims")
}

model SavedFilter {
  id        String   @id @default(cuid())
  name      String
  filters   Json
  userId    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@map("saved_filters")
}

enum ClaimStatus {
  OPEN
  RESOLVED
  CLOSED
}

enum UserRole {
  ADMIN
  BRANCH
}

// ===== 자동화 모듈 =====

model AutomationFlow {
  id              String    @id @default(cuid())
  name            String
  flowType        FlowType  @default(SMS)
  branchId        String
  isActive        Boolean   @default(true)
  triggerConfig   Json
  filterConfig    Json
  messageTemplate String?
  messageType     String?
  pointConfig     Json?
  createdBy       String
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  lastExecutedAt  DateTime?

  branch    Branch          @relation(fields: [branchId], references: [id], onDelete: Cascade)
  author    User            @relation(fields: [createdBy], references: [id])
  sendLogs  SmsSendLog[]
  pointLogs PointActionLog[]

  @@index([branchId])
  @@index([isActive])
  @@index([flowType])
  @@index([createdBy])
  @@map("automation_flows")
}

model SmsSendLog {
  id         String   @id @default(cuid())
  flowId     String
  customerId String
  phone      String
  message    String
  byteCount  Int
  pointUsed  Decimal  @db.Decimal(10, 2)
  sentAt     DateTime @default(now())
  status     String   @default("SUCCESS")

  flow AutomationFlow @relation(fields: [flowId], references: [id], onDelete: Cascade)

  @@index([flowId])
  @@index([customerId])
  @@index([sentAt])
  @@map("sms_send_log")
}

model PointActionLog {
  id         String    @id @default(cuid())
  flowId     String
  customerId String
  phone      String
  action     String
  amount     Int
  reason     String
  expiryDate DateTime? @db.Date
  executedAt DateTime  @default(now())
  status     String    @default("SUCCESS")

  flow AutomationFlow @relation(fields: [flowId], references: [id], onDelete: Cascade)

  @@index([flowId])
  @@index([customerId])
  @@index([executedAt])
  @@map("point_action_log")
}

model SmsPointHistory {
  id        String   @id @default(cuid())
  branchId  String
  balance   Int
  checkedAt DateTime @default(now())

  @@index([branchId])
  @@index([checkedAt])
  @@map("sms_point_history")
}

enum FlowType {
  SMS
  POINT
  SMS_POINT
}

// AI 대화 저장
model AiConversation {
  id        String      @id @default(cuid())
  userId    String
  branchId  String?
  title     String      @default("새 대화")
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt
  messages  AiMessage[]

  @@index([userId])
  @@index([createdAt])
  @@map("ai_conversations")
}

model AiMessage {
  id             String         @id @default(cuid())
  conversationId String
  role           String         // 'user' | 'assistant'
  content        String
  createdAt      DateTime       @default(now())
  conversation   AiConversation @relation(fields: [conversationId], references: [id], onDelete: Cascade)

  @@index([conversationId])
  @@map("ai_messages")
}
